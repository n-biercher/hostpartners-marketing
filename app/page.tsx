import { LandingPage, MarketingShell } from "@/marketing"

export const metadata = { title: "Hostpartners — Die modulare HR-Plattform" }

export default function HomePage() {
  return (
    <MarketingShell>
      <LandingPage />
    </MarketingShell>
  )
}
